# Raw data

This directory contains the original assessment workbook supplied for the dozer predictive maintenance exercise.

The workbook is treated as immutable input. Processing scripts should read from this directory and write derived data to `data/interim/` or `data/processed/`.
